version: "0.1.0"
services:
  eventSystemFe:
    build: ./
    container_name: calendar_fe_c
    ports:
      - "3000:3000"
    volumes:
      - ./app
      - ./app/node_modules
    stdin_open: true
    tty: true
